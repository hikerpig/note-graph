{
  "name": "note-graph",
  "version": "0.0.0",
  "scripts": {
    "dev": "cd demo && yarn serve",
    "build": "tsdx build --format umd --name NOTE_GRAPH"
  },
  "dependencies": {
    "d3": "^6.2.0",
    "force-graph": "^1.35.1"
  },
  "devDependencies": {}
}
