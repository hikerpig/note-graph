{
  "name": "note-graph",
  "version": "0.0.1",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "dev": "cd demo && yarn start",
    "build": "microbundle --name NOTE_GRAPH",
    "build-demo": "cd demo && yarn build",
    "ci-build": "cd demo && yarn && yarn build",
    "lint": "eslint src/**/*.ts"
  },
  "source": "src/index.ts",
  "umd:main": "dist/note-graph.umd.js",
  "files": [
    "dist"
  ],
  "typings": "dist/type.d.ts",
  "author": "hikerpig",
  "dependencies": {
    "d3": "^6.2.0",
    "force-graph": "^1.35.1"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.12.7",
    "@typescript-eslint/parser": "^4.9.0",
    "core-js": "3",
    "eslint": "^7.14.0",
    "lerna": "^3.22.1",
    "microbundle": "^0.12.4",
    "typescript": "4"
  }
}
