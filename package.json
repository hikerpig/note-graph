{
  "name": "note-graph",
  "version": "0.0.0",
  "scripts": {
    "dev": "cd demo && yarn serve",
    "build": "vite build"
  },
  "dependencies": {
  },
  "devDependencies": {
  },
  "eslintConfig": {
    "root": true,
    "env": {
      "node": true
    },
    "extends": [
      "eslint:recommended"
    ],
    "parserOptions": {
      "parser": "@typescript-eslint/parser"
    },
    "rules": {}
  }
}
